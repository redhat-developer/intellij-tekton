apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: foo
  namespace: tekton
spec:
  tasks:
    - name: step1
      taskRef:
        name: task1
      params:
        - name: foo2
          value: $(<caret>
    - name: step2
      taskRef:
        name: task1