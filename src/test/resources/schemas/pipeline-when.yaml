apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: app-deploy
spec:
  params:
    - name: p1
      default: value1
  tasks:
    - name: foo
      taskRef:
        kind: Task
        name: foo
      when:
        - input: myinput
          operator: in
          values:
          - valid
