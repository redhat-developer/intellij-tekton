apiVersion: tekton.dev/v1alpha1
kind: Task
metadata:
  name: foo
  namespace: tekton
spec:
  outputs:
    resources:
      - name: resource1
        type: image
      - name: resource2
        type: image
  steps:
    - args:
        - -c
        - echo hello world
      command:
        - /bin/bash
      image: fedora
      name: build-sources
      resources: {}
