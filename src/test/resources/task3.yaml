apiVersion: tekton.dev/v1alpha1
kind: Task
metadata:
  name: foo
  namespace: tekton
spec:
  inputs:
    params:
      - name: parm1
        type: string
        default: default value
  steps:
    - args:
        - -c
        - echo hello world
      command:
        - /bin/bash
      image: fedora
      name: build-sources
      resources: {}
